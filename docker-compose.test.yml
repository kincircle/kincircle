services:
  test-db:
    image: postgres:16-alpine
    tmpfs:
      - /var/lib/postgresql/data
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: kincircle_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test -d kincircle_test"]
      interval: 2s
      timeout: 5s
      retries: 10
